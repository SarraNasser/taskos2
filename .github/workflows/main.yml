on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "testing git actions"
        run: pwd  

      -name: "testing printing"
       run: echo sarra